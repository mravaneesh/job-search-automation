# Deterministic skill dictionary. Each canonical skill maps to one or more
# aliases. Extraction matches aliases case-insensitively on word boundaries
# against the plain-text job description. No LLM, no inference.

skills:
  Kotlin: [kotlin]
  Java: [java]
  "Jetpack Compose": ["jetpack compose", jetpack]
  Android SDK: ["android sdk", android]
  Swift: [swift]
  Python: [python]
  Go: [golang, " go "]
  Rust: [rust]
  "C++": ["c\\+\\+"]
  Scala: [scala]
  Ruby: [ruby]
  TypeScript: [typescript]
  JavaScript: [javascript]
  Node.js: ["node.js", nodejs]
  Spring: ["spring boot", spring]
  Django: [django]
  FastAPI: [fastapi]
  Flask: [flask]
  PostgreSQL: [postgresql, postgres]
  MySQL: [mysql]
  MongoDB: [mongodb]
  Redis: [redis]
  Kafka: [kafka]
  RabbitMQ: [rabbitmq]
  Elasticsearch: [elasticsearch]
  GraphQL: [graphql]
  gRPC: [grpc]
  REST: ["rest api", restful]
  Docker: [docker]
  Kubernetes: [kubernetes, k8s]
  AWS: [aws, "amazon web services"]
  GCP: [gcp, "google cloud"]
  Azure: [azure]
  Terraform: [terraform]
  PyTorch: [pytorch]
  TensorFlow: [tensorflow]
  scikit-learn: ["scikit-learn", sklearn]
  Pandas: [pandas]
  NumPy: [numpy]
  Spark: ["apache spark", pyspark, spark]
  Airflow: [airflow]
  LLM: ["large language model", llm]
  NLP: ["natural language processing", nlp]
